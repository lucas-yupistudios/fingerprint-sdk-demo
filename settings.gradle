pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url = uri("https://maven.pkg.github.com/biopass-id/face-sdk-android")
            credentials {
                username = "your-username"
                password = "your-token"
            }
        }
    }
}
rootProject.name = "Fingerprint SDK Demo"
include ':app'
